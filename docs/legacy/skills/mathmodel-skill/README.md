# Legacy Skill Shim

This repository no longer keeps a nested `skills/*/SKILL.md` file as an active entrypoint in `v1.2-alpha`.

The active skill entrypoint is the root [SKILL.md](../../../SKILL.md).

This file is retained only as legacy documentation after the Phase 1 identity reset from `mathmodel-skill` to `mathmodel-md-copilot`. It should not be used as a live skill entrypoint.
