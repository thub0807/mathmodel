# Tests

The current fixtures are legacy fixtures. They do not certify `v1.2-alpha` behavior by themselves.

## Current Status

- `tests/fixtures/*` mainly target older critique, scoring, or competition-loading logic.
- They are useful for historical reference and future migration work.
- They must not block the current alpha workflow.

## Follow-Up Direction

Future tests should focus on:
- protocol file completeness
- workspace template integrity
- result and traceability schema checks
- paper-generation contract checks
