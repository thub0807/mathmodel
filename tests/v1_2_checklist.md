# v1.2-alpha Checklist

- only one active `SKILL.md` exists at the repository root
- `SKILL.md` stays compact and points to workflow references
- active workflow references exist under `references/`
- Manual mode returns review file paths only
- AP mode writes `review_note.md` and `warnings.md`
- final outputs live under `workspace/output/final/`
- templates cover all workflow-required markdown and json artifacts; generated-only outputs such as `paper.pdf` and `results/run.log` are excluded from static templates
- scripts are optional only
- no active `decision_log` dependency remains in the workflow contract
- no active Friendly Mode dependency remains
