# Code Starters

- These starters are Python-only.
- Use them only when `project_contract.md` locks `implementation_language` to `python`.
- They are optional starting points, not required workflow scripts.
