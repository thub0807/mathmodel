# LaTeX Templates

These templates are retained for `v1.2-alpha` and will continue to support `paper_generation_protocol.md`.

## Current Role

- provide a paper-generation target for `paper.tex`
- offer competition-oriented layout assets where available
- remain optional until the paper assembly step

## Current Status

- `cumcm/` keeps the existing `cumcmthesis` assets
- `mcm/` keeps the existing article-style template
- `diangong/` keeps the existing article-style template

## Alpha Boundary

The templates are preserved, but not all interfaces are fully adapted yet to the new `workspace/output/` paper pipeline.

## TODO

- align template inputs with `workspace/output/paper.md`, `paper.tex`, and `source/`
- document the minimum assembly contract for each template
- keep template reuse compatible with `locked_numbers.md` and final-review requirements
