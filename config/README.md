# Config

Configuration files in this directory are reference data only.

## Current Status

- `dim_weights.json`
  - Legacy weighting data from the older multi-competition scoring flow.
  - Not a default input to `v1.2-alpha` rubrics.
  - Not required by the active workflow.
  - May be reused later as background judging context if a lightweight review helper is rebuilt.
