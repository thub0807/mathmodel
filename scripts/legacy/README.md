# Legacy Scripts

These scripts are retained for migration reference only.

- they are not required by the active `v1.2-alpha` workflow
- they still contain legacy `decision_log`, competition, or stage-scoring assumptions
- do not treat them as active workflow entrypoints
