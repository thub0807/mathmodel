# q6 Review Packet

## AP mode note

当前问题按 AP 模式自动推进，直接对 `q5` 候选做局部扰动稳健性审查。

## upstream context

- 已读取 `q1` 的综合指标与 short-term proxy 定义，确保稳健性口径不被写成寿命结论。
- 已读取 `q2` 的主预测模型与 uncertainty hook，作为邻域预测基础。
- 已读取 `q3` 的关键组分与交互作用结论，用来识别哪些候选可能只是局部尖峰。
- 已读取 `q4` 的可信域分层，用来区分高可信盆地与低可信风险区。
- 已读取 `q5` 的 top10 候选，用来执行最终稳健性筛查。
