# q5 Review Packet

## AP mode note

当前问题按 AP 模式自动推进，直接使用 `q2` 的预测模型与 `q4` 的可信域分层设计下一轮实验候选。

## upstream context

- 已读取 `q1` 的指标定义与 short-term proxy 限制，用来约束 `PI / W_1 / R_W` 的解释边界。
- 已读取 `q2` 的预测结果与 uncertainty hook，用来提供候选收益估计。
- 已读取 `q3` 的关键组分与交互作用解释，用来指导开发型与探索型候选的方向选择。
- 已读取 `q4` 的 trust tier 分层，用来限制候选风险。
